//1.removeChild()removeChild(): This method is used to remove a child element from its parent. You need a reference to both the parent and the child.:
let parent=document.querySelector('div');
let child1=document.querySelector('h1');
parent.removeChild(child1);

//2.remove(): This method removes the element directly from the DOM.
